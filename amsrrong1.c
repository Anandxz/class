#include<stdio.h>
#include<math.h>
int main()
{ 
 int a,b;
 printf("Enter the number of Digit you want ");
 scanf("%d",&a);                                 // Let 4      
 b=pow(10,a);                                    // 10000
 for(int i=pow(10,a-1);i<=(b-1);i++)             // 1000 to 9999
 {
    
 }
 

       
       
    
    return 0;
}